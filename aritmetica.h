
/** \brief muestra el menu y ejecuta las funciones internas.
 *
 * \param no recibe parámetros..
 * \return no tiene return , funcion tipo de dato void.
 *
 */
void menu(void);

/** \brief recibe dos datos y realiza la suma de los mismos.
 *
 * \param numeroA int
 * \param numeroB int
 * \return retorna la suma de los parámetros..
 *
 */
int sumarNumeros(int numeroA, int numeroB);

/** \brief recibe dos datos y realiza la resta de los mismos.
 *
 * \param numeroA int
 * \param numeroB int
 * \return retorna la resta de los parámetros..
 *
 */
int restarNumeros(int numeroA, int numeroB);

/** \brief recibe dos datos y realiza la multiplicación de los mismos.
 *
 * \param numeroA int
 * \param numeroB int
 * \return retorna la multiplicacion de los parámetros.
 *
 */
int multiplicarNumeros(int numeroA, int numeroB);

/** \brief recibe dos datos y realiza la division de los mismos.
 *
 * \param numeroA int
 * \param numeroB int
 * \return retorna la division de los parametros.
 *
 */
float dividirNumeros(int numeroA, int numeroB);

/** \brief recibe dos datos, factoriza cada uno.
 *
 * \param numeroA int
 * \param numeroB int
 * \return retorna el factorial de cada parametro.
 *
 */
int factorialNumero(int numeroA , int numeroB);
